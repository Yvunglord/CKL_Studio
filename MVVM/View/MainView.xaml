<UserControl x:Class="CKL_Studio.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CKL_Studio"
             mc:Ignorable="d" 
             d:MinHeight="450" d:MinWidth="800">
    <DockPanel LastChildFill="True" Background="#1e1e1e">
        <!-- Меню сверху -->
        <Menu DockPanel.Dock="Top" Background="#1e1e1e">
            <MenuItem Header="Файл" Foreground="White"/>
            <MenuItem Header="Править" Foreground="White"/>
            <MenuItem Header="Унарные" Foreground="White"/>
        </Menu>

        <!-- DataGridView снизу -->
        <DataGrid DockPanel.Dock="Bottom" Height="100" Margin="10, 10, 10, 10" Background="#373737"/>

        <!-- ListBox слева -->
        <ListBox Name="DataListBox" DockPanel.Dock="Left" Width="100" Margin="10, 30, 10,10" Background="#373737"/>

        <!-- ListView справа -->
        <ListView Name="SolutionExplorer" DockPanel.Dock="Right" Width="100" Margin="10, 30, 10, 10" Background="#191919"/>
        <DockPanel Margin="10, 30, 10, 10" Background="#2c2c2c"/>


    </DockPanel>
</UserControl>
